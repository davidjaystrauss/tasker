<h1>My Task Categories<h1>
	
	<% @lists.each do |list| %>
		<h2><%= link_to "#{list.name}", list_url(list) %></h2>
		<p><%= list.description %> - <%= link_to "Edit", edit_list_url(list) %> | <%= link_to "Delete", list_url(list), :method => :delete %> </p>
	<% end %>
	
		<%= link_to "Add new category", new_list_url %>